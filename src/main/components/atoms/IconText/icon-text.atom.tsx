import React from "react";

type IconText {
  icon?: boolean;
  inline?: boolean;
  colorIcon?: string;
  colorText?: string;
}

const IconText = ({icon, inline, colorIcon, colorText, ...props}: IconText) => {
  
}